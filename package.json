{
  "name": "payflow",
  "private": true,
  "description": "Repo-root tooling only (commit linting). App code lives in /services.",
  "scripts": {
    "prepare": "husky"
  },
  "devDependencies": {
    "@commitlint/cli": "^19.5.0",
    "@commitlint/config-conventional": "^19.5.0",
    "husky": "^9.1.7"
  }
}
